import React from 'react';

/**
 * Version component
 * @param {Object} props 
 * @returns 
 */
const Version = (props) => {
  return <span {...props}>{'v0.1.4'}</span>;
};

Version.propTypes = {};

export default Version;
